PORT=4000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/sarahah
JWT_SECRET=change_me_to_a_long_random_string
CLIENT_URL=http://localhost:5173
# Google OAuth Client ID (Web application). Required to accept anonymous senders.
# Create at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
# Optional comma-separated usernames to auto-promote to admin on boot
BOOTSTRAP_ADMIN_USERNAMES=
